# Blocked run: week of 2026-08-10

Same blocker as the week of 2026-08-03 (`brand-outreach/pending/2026-08-03-blocked.md`).
Re-tested this run: a raw TCP connection to the IMAP host in
`mail-creds.env` (`cloud-6653e5.managed-vps.net:993`) still times out
(`TimeoutError: [Errno 110] Connection timed out` after 15s) before any
login is attempted. Confirmed via `curl $HTTPS_PROXY/__agentproxy/status`
that this environment's egress proxy only relays HTTP(S)/CONNECT traffic —
raw IMAP is not in its supported protocol set. No mailbox credentials were
sent, no drafts were created anywhere.

This is now **two consecutive weeks** blocked on the same root cause. It
will keep failing every week until one of these changes:

1. The routine's execution environment is configured with an egress policy
   that allows raw TCP to `cloud-6653e5.managed-vps.net:993`, or
2. `mail-draft.py` is switched from IMAP APPEND to an HTTPS-based mailbox
   API for `caroline@runletters.com` (e.g. if that mailbox supports a REST/
   IMAP-over-HTTP bridge, or Google Workspace API if it's actually hosted
   there).

Note: this session has a Gmail MCP connector available, but it is not
known to be connected to the `caroline@runletters.com` mailbox specifically
(this routine was deliberately repointed away from a generic Gmail-drafts
flow to the `caroline@runletters.com` mailbox on 2026-07-26, see commit
`5a03d41`). Rerouting drafts to a different mailbox is a decision for
Caroline, not something this run makes unilaterally.

`brand-outreach/ledger.json` was NOT updated, since nothing was actually
drafted. Nothing has been sent.

## Brands ready to go (carried over from 2026-08-03, still not in ledger, contacts not re-verified this week)

Full research and drafted email bodies were already completed on
2026-08-03 and remain valid (no reason to believe the brands or contact
addresses have changed in a week):

1. **Satisfy Running** — satisfyrunning.com — contact@satisfyrunning.com —
   "That SoftCell fabric aging with wear"
2. **MudGear** — mudgear.com — support@mudgear.com — "Your merino OCR
   socks and the rope burn problem"
3. **Precision Fuel & Hydration** — precisionhydration.com —
   hello@pfandh.com — "The 45 minute sweat test idea"
4. **Spring Energy** — myspringenergy.com — contact@myspringenergy.com —
   "Awesome Sauce and the no maltodextrin thing"

See `brand-outreach/pending/2026-08-03-blocked.md` for the full email
bodies, ready to paste into `mail-draft.py --body-file` (or send manually)
the moment the mailbox path works.
