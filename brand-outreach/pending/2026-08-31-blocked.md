# Blocked run: week of 2026-08-31

Same blocker as the weeks of 2026-08-03, 2026-08-10, and 2026-08-24
(`brand-outreach/pending/2026-08-03-blocked.md`,
`brand-outreach/pending/2026-08-10-blocked.md`,
`brand-outreach/pending/2026-08-24-blocked.md`). This is the fourth
consecutive week this exact root cause has hit the routine.

Re-tested this run: a raw TCP connection to the IMAP host in
`mail-creds.env` (`cloud-6653e5.managed-vps.net:993`) still times out
(`TimeoutError: timed out`, confirmed via a direct Python socket connect
test, no login attempted). `curl $HTTPS_PROXY/__agentproxy/status` confirms
this environment's egress proxy still only relays HTTP(S)/CONNECT traffic,
with a `noProxy` bypass list that does not cover this mail host. No
mailbox credentials were sent, no drafts were created anywhere.

Nothing about this blocker has changed in a month. It will keep failing
every week until one of these changes:

1. The routine's execution environment is configured with an egress policy
   that allows raw TCP to `cloud-6653e5.managed-vps.net:993`, or
2. `mail-draft.py` is switched from IMAP APPEND to an HTTPS-based mailbox
   API for `caroline@runletters.com`.

Note: this session again has a Gmail MCP connector available, but as with
the last three blocked runs, it is not known to be connected to the
`caroline@runletters.com` mailbox specifically, and this routine was
deliberately repointed away from a generic Gmail-drafts flow to the
`caroline@runletters.com` mailbox on 2026-07-26 (commit `5a03d41`).
Rerouting drafts to a different mailbox is still a decision for Caroline,
not something this run makes unilaterally.

`brand-outreach/ledger.json` was NOT updated, since nothing was actually
drafted. Nothing has been sent.

## No new research done this run

The 7 brands researched across the three prior blocked runs are all still
absent from the ledger and still look like solid candidates: District
Vision, Precision Fuel & Hydration, Neversecond, and Trail Butter (from
2026-08-24, full drafted bodies there), plus Satisfy Running, MudGear, and
Spring Energy (from 2026-08-03, full drafted bodies there). Since none of
them could be drafted anyway, this run did not duplicate that research or
draft a fifth batch on top of an already-unusable backlog. Once the mailbox
path is fixed, these 7 are ready to go through `mail-draft.py
--body-file` as-is; a future run should sanity-check that each contact
address and subject is still current before sending.
